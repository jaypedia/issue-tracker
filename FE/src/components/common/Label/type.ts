export type LabelProps = {
  title: string;
  size: 'large' | 'small';
  backgroundColor: string;
  textColor: string;
};

export type LabelStyle = {
  size: 'large' | 'small';
  backgroundColor: string;
  textColor: string;
};
