spring:
  jpa:
    hibernate:
      ddl-auto: create

  sql:
    init:
      mode: never

oauth2:
  client-id: clientId
  client-secret: clientSecret
  redirect-uri: redirectUrl
  token-uri: tokenUrl
  user-info-uri: userInfoUrl

jwt:
  secretKey: HRlELXqpSB
  refreshSecretKey: HRlELXqpSB
  accessTokenExpiry: 10
  refreshTokenExpiry: 10
